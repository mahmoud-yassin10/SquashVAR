# --- Never commit large media ---
*.mp4
*.avi
*.mov
*.mkv
*.png
*.jpg
*.jpeg
*.webp

# --- Datasets / raw data ---
data/
datasets/
label_preview/
label_pool_v1/

# --- Temp / local tooling ---
tmp/
.ultra_cfg/
__pycache__/
*.pyc
.ipynb_checkpoints/

# --- Runs: default ignore everything ---
runs/**

# --- Runs: allow only small text outputs you want ---
!runs/**/*.yaml
!runs/**/*.yml
!runs/**/*.json
!runs/**/*.txt
!runs/**/*.csv
!runs/**/*.log

# --- Runs: explicitly block common large artifacts inside runs ---
runs/**/weights/
runs/**/labels/
runs/**/images/
runs/**/crops/
runs/**/tracks/
runs/**/predictions/
runs/**/val_batch*.jpg
runs/**/val_batch*.png
runs/**/PR_curve.png
runs/**/P_curve.png
runs/**/R_curve.png
runs/**/F1_curve.png
runs/**/confusion_matrix*.png

# --- Figures: allow graphs but not extracted qualitative frames (optional) ---
# If you want to keep ONLY charts (png) in figs, remove the global *.png ignore above and use:
# figs/**/*
# !figs/**/*.png

# --- Keep weights at root (you said yes) ---
!*.pt
